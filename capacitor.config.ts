import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.appfit.app",
  appName: "FiT календарь тренировок",
  webDir: "apps/frontend/dist",
};

export default config;
